require('source-map-support').install();

import assert from 'assert';

import Solver2D from '../dist/solvers/solver2d';

describe('Solver2D', () => {
    describe('solve', () => {
//        it('end to end test', () => {
//            var itemsw = [2, 3 , 4];
//            var itemsh = [2, 3 , 4];
//            var demands = [20, 10, 20];
//            var stocksw = [5, 6];
//            var stocksh = [5, 6];
//
//            var solver = new Solver2D(stocksw, stocksh, itemsw, itemsh, demands);
//            var res = solver.solve();
//
//            assert.notEqual(res, null);
//
//            assert.equal(res.cost, 170);
//            assert.deepEqual(res.counts, [10, 10, 0]);
//            assert.deepEqual(res.activities.map(a => a.activities), [[1, 0, 1], [1, 1, 1], [0, 0, 2]]);
//            assert.deepEqual(res.activities.map(a => a.stockIdx), [1, 2, 2]);
//            checkDemands(items, demands, res);
//        });

//        it('end to end test #2', () => {
//            var itemsw = [2, 3 , 4];
//            var itemsh = [2, 3 , 4];
//            var demands = [1, 1, 1];
//            var stocksw = [5];
//            var stocksh = [5];
//
//            var solver = new Solver2D(stocksw, stocksh, itemsw, itemsh, demands);
//            var res = solver.solve();
//
//            assert.notEqual(res, null);
//        });
    });
});
